{
  "name": "stellar-nebula-nomad-subgraph",
  "version": "1.0.0",
  "description": "Event indexer for Stellar Nebula Nomad smart contract",
  "repository": "https://github.com/yourusername/stellar-nebula-nomad",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "create-local": "graph create --node http://localhost:8020 stellar-nebula-nomad",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 stellar-nebula-nomad",
    "deploy-hosted": "graph deploy --product hosted-service your-username/stellar-nebula-nomad"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
