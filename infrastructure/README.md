# Infrastructure as Code

This directory contains the Terraform configuration for provisioning the infrastructure of the Stellar Nebula Nomad project.

## Requirements
- Terraform >= 1.5
- AWS CLI configured with appropriate credentials

## Usage
1. `terraform init` to initialize the directory.
2. `terraform plan` to see the proposed changes.
3. `terraform apply` to apply the configuration.
